# Copia este archivo a .env.migracion y pon las dos URLs EXTERNAS que da
# Render en el panel de cada base ("External Database URL").
# .env.migracion no se sube al repositorio.

# La base que tienes hoy, la gratuita.
ORIGEN="postgresql://usuario:clave@host.oregon-postgres.render.com/basevieja"

# La base nueva, de pago, recién creada y vacía.
DESTINO="postgresql://usuario:clave@host.oregon-postgres.render.com/basenueva"
